name: Snyk Container Scan and SARIF Upload
description: Run Snyk container scan, normalize SARIF severity fields, upload SARIF, and notify Slack on failure.

inputs:
  image:
    description: Fully qualified image reference to scan.
    required: true
  dockerfile:
    description: Dockerfile path for Snyk context.
    required: true
  category:
    description: SARIF category for GitHub code scanning upload.
    required: true
  snyk_token:
    description: Snyk API token.
    required: true
  slack_token:
    description: Slack bot token for failure notifications.
    required: true
  channel_id:
    description: Slack channel ID for failure notifications. Set to NO_SLACK to disable.
    required: true
  subproject:
    description: Subproject name used in Slack notification.
    required: false
    default: ""
  scan_type:
    description: Scan context used in Slack notification.
    required: false
    default: container-image
  slack_include_summary:
    description: Include Snyk summary in Slack message when available.
    required: false
    default: "false"
  workflow:
    description: GitHub workflow name.
    required: true
  repository:
    description: GitHub repository slug.
    required: true
  ref_name:
    description: Git ref name.
    required: true
  run_id:
    description: GitHub run ID.
    required: true
  job_status:
    description: Current job status.
    required: true
  dockerfile_dir:
    description: Matrix dockerfile directory.
    required: true
  tag:
    description: Tag to include in Slack notification.
    required: true

runs:
  using: composite
  steps:
    - name: Run Snyk container scan
      id: snyk-scan
      continue-on-error: true
      uses: snyk/actions/docker@9adf32b1121593767fc3c057af55b55db032dc04 # v1.0.0
      env:
        SNYK_TOKEN: ${{ inputs.snyk_token }}
      with:
        image: ${{ inputs.image }}
        args: --severity-threshold=high --file=${{ inputs.dockerfile }}

    - name: Verify Snyk scan execution
      shell: bash
      run: |
        if [[ ! -s snyk.sarif ]]; then
          echo "Snyk scan did not complete successfully (missing snyk.sarif)."
          exit 1
        fi

    - name: Prepare SARIF for upload
      if: ${{ hashFiles('snyk.sarif') != '' }}
      shell: bash
      run: |
        set -euo pipefail

        jq '
          def severity_from_level($level):
            if $level == "error" then "8.0"
            elif $level == "warning" then "5.0"
            elif $level == "note" then "3.0"
            else "0.0" end;

          def normalize_any_severity($value):
            if $value == null then "0.0"
            elif ($value | type) == "number" then ($value | tostring)
            elif ($value | type) == "string" then
              if ($value | test("^[0-9]+(\\.[0-9]+)?$")) then $value
              elif (($value | ascii_downcase) == "critical") then "9.0"
              elif (($value | ascii_downcase) == "high") then "8.0"
              elif (($value | ascii_downcase) == "medium") then "5.0"
              elif (($value | ascii_downcase) == "low") then "3.0"
              else "0.0" end
            else "0.0" end;

          def normalize_rule:
            .properties = (.properties // {})
            | .defaultConfiguration = (.defaultConfiguration // {})
            | .properties["security-severity"] =
                (
                  .properties["security-severity"] as $sev
                  | if $sev == null then severity_from_level(.defaultConfiguration.level)
                    else normalize_any_severity($sev)
                    end
                );

          def fix_security_severity_fields:
            if type == "object" then
              (with_entries(.value |= fix_security_severity_fields)
               | if has("security-severity") then
                   .["security-severity"] = normalize_any_severity(.["security-severity"])
                 else
                   .
                 end)
            elif type == "array" then
              map(fix_security_severity_fields)
            else
              .
            end;

          .runs |= map(
            .tool = (.tool // {})
            | .tool.driver = (.tool.driver // {})
            | .tool.driver.rules = ((.tool.driver.rules // []) | map(normalize_rule))
            | .tool.extensions = ((.tool.extensions // []) | map(
                .rules = ((.rules // []) | map(normalize_rule))
              ))
          )
          | fix_security_severity_fields
        ' snyk.sarif > snyk-upload.sarif

    - name: Build Snyk summary
      id: snyk-summary
      if: ${{ hashFiles('snyk.sarif') != '' }}
      shell: bash
      run: |
        set -euo pipefail

        total="$(jq '[.runs[].results[]?] | length' snyk.sarif)"
        high="$(jq '[.runs[].results[]? | select(.level == "error")] | length' snyk.sarif)"
        medium="$(jq '[.runs[].results[]? | select(.level == "warning")] | length' snyk.sarif)"
        low="$(jq '[.runs[].results[]? | select(.level == "note")] | length' snyk.sarif)"

        echo "results_count=${total}" >> "$GITHUB_OUTPUT"
        echo "summary=Total: ${total}, High: ${high}, Medium: ${medium}, Low: ${low}" >> "$GITHUB_OUTPUT"

    - name: Upload Snyk SARIF
      id: upload-sarif
      if: ${{ always() && hashFiles('snyk-upload.sarif') != '' }}
      uses: github/codeql-action/upload-sarif@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
      with:
        sarif_file: snyk-upload.sarif
        category: ${{ inputs.category }}

    - name: Snyk slack notification
      uses: ministryofjustice/hmpps-github-shared-actions/.github/actions/slack_snyk_scan_notification@8814948063257e87e324381e273af464243de470 # v1.0.18
      if: (failure() || steps.snyk-scan.outcome == 'failure' || steps.snyk-summary.outputs.results_count > 0) && inputs.channel_id != 'NO_SLACK'
      with:
        title: Snyk
        warningOnly: ${{ steps.upload-sarif.outcome == 'success' }}
        channel_id: ${{ inputs.channel_id }}
        SLACK_BOT_TOKEN: ${{ inputs.slack_token }}
        subproject: ${{ inputs.subproject }}
        scan_context: ${{ inputs.scan_type }}
        summary: ${{ inputs.slack_include_summary && steps.snyk-summary.outputs.summary || '' }}

